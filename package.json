{
  "name": "ai-retrofit",
  "version": "1.0.0",
  "description": "Smart intercom control system with web and mobile apps",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:mobile\"",
    "dev:web": "npm run build:shared && cd apps/web && npm run dev",
    "dev:mobile": "npm run build:shared && cd apps/mobile && npm run start",
    "build": "npm run build:shared && npm run build:web",
    "build:shared": "cd packages/shared && npm run build",
    "build:web": "cd apps/web && npm run build",
    "install:all": "npm install",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules packages/*/dist",
    "type-check": "npm run type-check:shared && npm run type-check:web && npm run type-check:mobile",
    "type-check:shared": "cd packages/shared && npm run type-check",
    "type-check:web": "cd apps/web && npm run type-check",
    "type-check:mobile": "cd apps/mobile && npm run type-check"
  },
  "dependencies": {
    "@ai-retrofit/shared": "*"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
